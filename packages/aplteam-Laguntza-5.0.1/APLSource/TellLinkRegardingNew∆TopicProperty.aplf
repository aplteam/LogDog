 {r}←TellLinkRegardingNew∆TopicProperty where;cmd;path
⍝ Tell Link
 r←0
 ∘∘∘  ⍝TODO⍝
 :If CheckOnEditFixForSaltHandler ⍬
 :AndIf 0≠≢∆Ref.p.saltFolder
     path←GetSaltPath where
     cmd←(⍕where),'.∆TopicProperties ',path,'\∆TopicProperties.dyalog'
     {}⎕SE.SALT.Save cmd
     r←1
 :EndIf
