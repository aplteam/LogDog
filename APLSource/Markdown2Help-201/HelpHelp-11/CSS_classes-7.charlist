[DATA]:index='Layout ⋄ Built-in classes ⋄ CSS classes ⋄ Classes, CSS'

# CSS classes

The default CSS Markdown2Help comes with offer a few classes useful to be assigned to objects

center
: For tables, images and paragraphs.

small
: Just for paragraphs.

See →[*.Images] for an example.
