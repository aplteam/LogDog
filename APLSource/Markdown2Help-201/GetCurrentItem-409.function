 GetCurrentItem←{
     n←⍵
     {⍵<⍴n.tree.SelItems:⍵ ⋄ ⍬}⍬⍴n.tree.SelItems⍳1
⍝ Returns the number of the current item as a scalar
⍝ or ⍬ if there is no item selected currently.
⍝ Yes, that DID happen in the past.
 }
